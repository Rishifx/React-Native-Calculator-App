import colors from './colors';

export default {
    primary: [colors.primaryStart, colors.primaryMid, colors.primaryEnd],
    buttonGlass: [colors.glassBackground, colors.glassBackgroundHover],
    modal: [colors.primaryStart, colors.primaryMid],
};
